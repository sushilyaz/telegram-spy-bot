# Локальная разработка - НЕ использовать в продакшене!
# Запуск: ./gradlew bootRun --args='--spring.profiles.active=local'

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spybot
    username: spybot
    password: devpassword

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Для локальной разработки эти значения должны быть заданы через переменные окружения
# или .env файл. Ниже приведены placeholder'ы.
telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:8537512343:AAHfuzTWDUMdsZ5eDE6q9VBEUnDTX_bpo8Y}
    username: ${TELEGRAM_BOT_USERNAME:shpion_test1_bot}

encryption:
  # Тестовый ключ - ЗАМЕНИТЕ НА СВОЙ!
  secret-key: ${ENCRYPTION_SECRET_KEY:MO9ttWahdQo+T4id4V9hs3SXMBv6JE4OkK26UTXoH64=}

logging:
  level:
    com.spybot: DEBUG
    org.hibernate.SQL: DEBUG
